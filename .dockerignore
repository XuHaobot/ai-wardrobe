# 密钥与本地环境
# 注意：backend/.env 必须进镜像 —— 容器里的 DashScope/高德/火山 Key 全靠它，
# 缺失时容器内所有 AI 功能（推荐/试穿/天气）全部不可用。根目录 .env 才是被排除的。
.env
.env.*
!.env.example
!.env.server.example
!backend/.env

# 前端依赖与构建缓存（构建阶段会重新安装）
ai-outfit-recommender/node_modules
ai-outfit-recommender/dist
ai-outfit-recommender/.vite

# Python 缓存
__pycache__
*.pyc
.pytest_cache

# 运行时可变数据（容器内重建，不进镜像）
# 注意：backend/uploads 必须进镜像 —— 游客演示衣橱的图片与模特底图（男/女.png）都在这里，
# 排除后 seed_demo 找不到图，游客进入将是空衣橱、试穿页底图 404
backend/data/chroma
backend/data/*.db
backend/uploads/tryon_*
backend/__pycache__

# 杂项
.git
.gitignore
*.log
debug_*.py
check_*.py
quick_*.py
